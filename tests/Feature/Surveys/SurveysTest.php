<?php

test('a user with access to admin module can create a survey', function () {
	$this->assertTrue(true);
	// get a user with admin access

	// Create a survey & post to the reevant endpoint

	// Check the survey is present in the DB

	// ? redirect?

});

test('a user *without* access to admin module cannot create a survey', function () {
	$this->assertTrue(true);
	// get a user without admin access

	// Create a survey & post to the reevant endpoint

	// Check the survey is present in the DB

	// ? redirect?


});
